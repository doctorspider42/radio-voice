#pragma once

/// Resource identifiers shared by res/app.rc and the code that loads them.
///
/// The shell shows the icon with the numerically lowest identifier as the
/// executable's icon, so any icon added later has to stay above this one.
#define IDI_APPICON 101
